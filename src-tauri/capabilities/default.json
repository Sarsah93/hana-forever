{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Mascot window, its action panel window and the usage guide window.",
  "windows": [
    "main",
    "panel",
    "guide"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
